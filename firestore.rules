rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access to all documents for now (development mode)
    // TODO: Add proper authentication and authorization rules
    match /{document=**} {
      allow read, write: if true;
    }
  }
}